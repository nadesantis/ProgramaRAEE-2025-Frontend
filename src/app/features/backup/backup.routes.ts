// src/app/features/backup/backup.routes.ts
import { Routes } from '@angular/router';
import { BackupPageComponent } from './ui/backup-page/backup-page.component';

export const BACKUP_ROUTES: Routes = [
  { path: '', component: BackupPageComponent }
];
